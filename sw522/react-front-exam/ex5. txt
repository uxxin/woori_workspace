DONE 항목은 TODO와 IN_PROGRESS 아래로 정렬되도록 하는 기능 구현

TodoBody.jsx에서 sort 함수를 이용해 구현

  const sortedTodos = filteredTodos.sort((a, b) => {
    if (a.category === "DONE" && b.category !== "DONE") return 1;
    if (a.category !== "DONE" && b.category === "DONE") return -1;
    return 0;
  });